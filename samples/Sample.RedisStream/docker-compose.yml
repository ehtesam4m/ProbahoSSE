name: probaho-stream

services:
  # ── Infrastructure ───────────────────────────────────────────────────────────
  redis:
    image: redis:7.2-alpine
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── IoT Sensor Simulator ─────────────────────────────────────────────────────
  # Standalone HTTP producer — POSTs sensor readings to /ingest on api-1.
  # Fully decoupled from Redis: swap backplanes without touching this service.
  simulator:
    build:
      context: ../..
      dockerfile: samples/Common.IoTSensorSimulator/Dockerfile
    depends_on:
      - stream-api-1
    environment:
      Simulator__IngestUrl: http://api-1:8080/ingest
      Simulator__RetryDelayMs: "3000"
    restart: unless-stopped

  # ── API Instance 1 ───────────────────────────────────────────────────────────
  stream-api-1:
    build:
      context: ../..
      dockerfile: samples/Sample.RedisStream/Dockerfile
    depends_on:
      redis:
        condition: service_healthy
    environment:
      Redis__ConnectionString: redis:6379
      ASPNETCORE_URLS: http://+:8080
    networks:
      default:
        aliases:
          - api-1
    restart: unless-stopped

  # ── API Instance 2 ───────────────────────────────────────────────────────────
  stream-api-2:
    build:
      context: ../..
      dockerfile: samples/Sample.RedisStream/Dockerfile
    depends_on:
      redis:
        condition: service_healthy
    environment:
      Redis__ConnectionString: redis:6379
      ASPNETCORE_URLS: http://+:8080
    networks:
      default:
        aliases:
          - api-2
    restart: unless-stopped

  # ── Nginx Reverse Proxy + HTML ───────────────────────────────────────────────
  nginx:
    image: nginx:1.27-alpine
    ports:
      - "8080:8080"
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../nginx/html:/usr/share/nginx/html:ro
    depends_on:
      - stream-api-1
      - stream-api-2
    restart: unless-stopped
